---
name: like-you
description: One-shot bootstrap of a ~/Business/.claude/ workspace from 90 days of Gmail, Calendar, Drive, Docs, and Sheets. Use when the user says "bootstrap my business", "set up like-you", "תקים לי את הסוכן", "תבנה לי context", "map my business into Claude Code", or similar phrases. Produces CLAUDE.md, a context/ entity graph (people, companies, projects), a voice.md style profile distilled from 50 real sent emails, 5-10 business-specific skills split into content and judgment, and an automations.md declarative spec for proactive triggers. Hebrew-first, English fallback. Local files (.mbox, .ics, folder paths) accepted when MCPs are missing. Default vault path is ~/Business. Run only once per workspace; refuses to run if a workspace already exists unless --force is passed.
---

# like-you — bootstrap an agent that runs your business like you would

You are about to set up a Claude Code workspace at `~/Business/.claude/` (or a path the user chooses) that turns 90 days of the user's Gmail, Calendar, Drive, Docs, and Sheets into a memory graph + a writing voice + 5–10 business-specific skills + a proactive automation spec.

**The promise**: two slash commands → three short questions → 15 minutes → ready. **The skill adapts to the user invoking it.** It reads THEIR Gmail/Drive/Calendar, extracts THEIR brand from THEIR own docs, distills THEIR voice from THEIR sent mail, generates skills tuned to THEIR business. Zero brand values are hardcoded.

You speak Hebrew first, English as fallback. Detect the user's locale: if `LANG`/`LC_ALL` contains `he_IL`, OR ≥40% of the user's sent mail is Hebrew, OR the user opens the conversation in Hebrew → use Hebrew. Otherwise English. Always show both languages on the first prompt; lock to the chosen one after.

---

## What you produce

```
~/Business/.claude/
├── CLAUDE.md                         # constitution; read first by every future session
├── automations.md                    # declarative trigger spec (v0.1 = doc; v0.2 = daemon)
├── context/
│   ├── people/{slug}.md              # one file per person you actually work with
│   ├── companies/{slug}.md
│   ├── projects/{slug}.md
│   └── voice.md                      # writing voice from 50 sent emails
├── skills/
│   ├── content/                      # CONTENT skills — produce artifacts
│   │   ├── reply-email/SKILL.md
│   │   ├── draft-proposal/SKILL.md
│   │   ├── design-deck/SKILL.md
│   │   └── meeting-followup/SKILL.md
│   └── judgment/                     # JUDGMENT skills — produce decisions
│       ├── triage-inbox/SKILL.md
│       ├── when-to-escalate/SKILL.md
│       └── prioritize/SKILL.md
├── raw/                              # immutable 90-day snapshot
│   ├── gmail/{date}-{slug}.md
│   ├── calendar/{date}-{slug}.md
│   └── drive/{date}-{slug}.md
└── _meta/
    ├── log.md                        # what was extracted, what was excluded, why
    └── confidence.json               # mode + per-source counts
```

Two outputs sit on top of this: **`/context`** (the memory, slow-changing, one file per entity) and **`/skills`** (the capabilities, fast-changing, each skill knows how to read the context and write in voice).

---

## When NOT to run

If `~/Business/.claude/CLAUDE.md` already exists, **refuse**. Print:

> **HE:** ` ~/Business/.claude קיים כבר. אם רוצים להריץ מחדש - תפעיל אותי שוב עם --force, אבל זה ידרוס את הכל. עדיף לערוך נקודתית.`
> **EN:** `A workspace already exists at ~/Business/.claude. Re-running will overwrite everything. Pass --force to proceed, or edit files in place.`

Same refusal if the user is in a directory that's already a Claude Code project (a `CLAUDE.md` sits at `$PWD/CLAUDE.md` AND it doesn't say `generated by like-you`).

---

## Phase 0 — Pre-flight (≤2 min budget)

Run these checks in parallel. Do not move on until all 5 MCP probes return or time out at 8s each.

### Step 0.1 — Probe MCPs (parallel)

| MCP | Probe | Required? |
|---|---|---|
| Gmail | `mcp__claude_ai_Gmail__list_labels` + `search_threads in:sent newer_than:90d` (capped at 1 page) | Required (or local `.mbox`/`.eml` fallback) |
| Calendar | `mcp__claude_ai_Google_Calendar__authenticate` status + `list events timeMin=-90d maxResults=10` | Required (or local `.ics` fallback) |
| Drive | `mcp__claude_ai_Google_Drive__list_recent_files pageSize=5` | Recommended |
| Docs | `mcp__claude_ai_Google_Drive__search_files mimeType=application/vnd.google-apps.document` pageSize=5 | Recommended |
| Sheets | `mcp__claude_ai_Google_Drive__search_files mimeType=...spreadsheet` pageSize=3 | Optional |
| Slack | `mcp__claude_ai_Slack__list_channels` if available (else skip — Slack MCP not yet universal) | Optional. If connected, list user's channels, filter to ones with ≥5 messages from user in 90d, sample top 3 channels' last 50 messages. Local fallback: Slack workspace export (`.zip` from Settings → Workspace settings → Import/Export Data) — parse the JSON files inside. |

Capture per-source: `connected: bool`, `count_in_window: int`, `latency_ms: int`, `owner_email: str`. Write to `_meta/confidence.json` as you go.

### Step 0.1b — Cross-account mismatch check (CRITICAL)

Many users have multiple Google accounts (personal Gmail + work Workspace, founder@x.com + founder.personal@gmail.com). When the MCPs are connected to different accounts, the skill produces a garbage `context/people/` graph because "you" appears as two identities — and worse, personal correspondence (insurance, medical, family) bleeds into the business knowledge base.

After Step 0.1 returns, extract the user's identity from each connected source:
- **Gmail**: the `sender:` address in the first `in:sent` thread.
- **Drive**: the `owner:` field on the first `list_recent_files` result.
- **Calendar**: the authenticated account email if probe succeeded.

If any two differ → **STOP** and ask one prompt before going further:

> **HE:** "שמתי לב לחיבורים מ-2 חשבונות גוגל שונים: **(א) {gmail-email}** ב-Gmail, **(ב) {drive-owner}** ב-Drive. איזה מהם החשבון העסקי שלך? אעבוד רק עם המקורות של החשבון הזה — אחרת תקבל גרף של 'שני אנשים שונים' כשבעצם זה אתה."
>
> **EN:** "I detected two different Google accounts: (a) {gmail-email} on Gmail, (b) {drive-owner} on Drive. Which is your business identity? I'll only ingest from that account's sources — otherwise the context graph treats 'you' as two different people."

Lock the answer into `_meta/confidence.json` as `business_identity:` and **drop all sources not associated with that account.** If the user says "both are mine — merge", explicitly merge with `aliases:` frontmatter on the user's own person page and log the choice to `_meta/log.md`. Default to drop, not merge — the personal account usually has data the user does NOT want in business context.

### Step 0.2 — Determine operating mode

Apply thresholds against the 90-day window:

| Mode | Gmail sent | Calendar events | Drive docs |
|---|---|---|---|
| `full` | ≥30 | ≥10 | ≥5 |
| `preliminary` | ≥10 | ≥3 | any |
| `cold-start` | <10 OR no MCP/fallback | — | — |

Mode is stamped into `CLAUDE.md`'s frontmatter so every downstream session sees the caveat. Preliminary mode skips per-client skill generation; cold-start writes only `CLAUDE.md` stub + `voice.md` from the user's pasted sample + a `seed-questions.md`.

### Step 0.3 — Handle missing/sparse sources (local-file fallback)

For each MCP that's missing or below threshold, ask **one** prompt (not a wall of text). Bilingual on first turn, then lock language:

**Gmail missing or sparse:**
> **HE:** "אין לי גישה ל-Gmail או שמצאתי פחות מ-30 מיילים. שתי אפשרויות: **(א)** תריצו `/plugin marketplace add anthropic-marketplace/gmail` ותפעילו אותי שוב, או **(ב)** תייצאו `.mbox` מ-Google Takeout (או תיקייה של `.eml`) ותדביקו לי את הנתיב. שתי הדרכים שקולות. מה נוח?"
>
> **EN:** "Gmail isn't reachable, or fewer than 30 sent messages found. Two options: (a) connect the Gmail MCP and rerun, or (b) drop a `.mbox` export (Google Takeout) or a folder of `.eml` files on disk and paste the path. Either works. Which?"

**Calendar missing (most common — Calendar MCP requires separate OAuth from Gmail):**

The Calendar MCP returns an error like `"Ask the user to run /mcp and select claude.ai Google Calendar to authenticate"` when not authed. **Surface the in-app recovery first, fallback second.** Print:

> **HE:** "Calendar לא מחובר. 30 שניות לתקן: הקלידו `/mcp` בסשן של Claude Code, בחרו `claude.ai Google Calendar`, השלימו OAuth בדפדפן, חזרו אליי עם 'המשך'. אם אין לכם Google Calendar — דבר אחר, תייצאו `.ics` (Settings → Export) ותדביקו לי את הנתיב."
>
> **EN:** "Calendar isn't connected. 30-second fix: in your Claude Code session, type `/mcp`, select `claude.ai Google Calendar`, complete OAuth in browser, come back and say `continue`. No Google Calendar? Export `.ics` (Settings → Export) and paste the path."

If user says `continue`, re-probe Calendar. If still missing or user picks the fallback, parse the `.ics` with the `ics` library (`pip install ics`); filter `ORGANIZER == user_email`.

**Drive missing:** ask for a folder path. Walk recursively for `.md`, `.docx`, `.pdf`, `.txt`. Skip files >5MB. Use `python-docx` for docx, `pdfplumber` for pdf (text only — flag scanned PDFs and skip).

**Sheets missing:** ask for a folder of `.csv`/`.xlsx`. Use `csv` stdlib + `openpyxl`. Sniff headers for `name|email|company|amount` columns.

**Slack missing:** Slack MCP isn't universally available yet — if no MCP exposes Slack tools, ask whether the user wants to skip Slack ingest or paste a workspace export (`.zip` from Settings → Workspace settings → Import/Export Data). Parse via Python `zipfile` + per-channel JSON. If user says "skip Slack", just note it in `_meta/log.md` and continue. Slack is **optional**, not required.

**Nothing at all:** cold-start. Skip to Phase 4 after the 3 orientation questions, accept a pasted voice sample (≥200 words), write a stub `CLAUDE.md` and `seed-questions.md` with 8 questions whose answers let a future re-run produce something real.

### Step 0.4 — Four orientation questions

Ask exactly four. No more. These calibrate the extraction AND determine which pipeline runs.

**Q1 (CRITICAL — branches the whole skill):**
> **HE:** "מה האג'נדה? **(א) עסק** — אני רוצה סוכן שמכיר לקוחות, פרויקטים, ועובד כמוני, **(ב) אישי** — אני רוצה סוכן שיעזור לי לנהל את החיים הפרטיים (משפחה, חברים, מטלות), **(ג) שניהם** — חיים מעורבבים, רוב ההתכתבות בחשבון אחד."
>
> **EN:** "What's the use case? (a) **Business** — I want an agent that knows my clients, projects, and writes like me. (b) **Personal** — I want help managing personal life (family, friends, errands). (c) **Both** — mixed life, most correspondence in one account."

Lock answer into `_meta/confidence.json` as `use_case: business | personal | hybrid`. This determines what gets extracted and what skills get generated:

| Use case | Phase 2 extracts | Phase 5 generates | Skip-list bias |
|---|---|---|---|
| `business` | people (relationship=client/partner/vendor), companies, projects | full catalog (5-10 skills, content + judgment) | aggressive skip of personal/family/medical/legal |
| `personal` | people (close contacts only, ≥3 interactions), NO companies, NO projects | minimal: just `reply-email` + `triage-inbox` + `prioritize`. Skip everything else. | aggressive skip of work/invoice/contract/proposal terms |
| `hybrid` | both, with explicit `personal_or_business:` tag on every entity | full catalog BUT every generated skill has a `scope_filter:` for personal vs business mail at runtime | both skip-lists merged; rely heavily on per-thread classification |

The skill MUST refuse to proceed if the user picks `personal` AND the connected account has clear business signal (e.g., ≥3 distinct corporate domains in sent mail) — it asks them to confirm: "Your account looks like it has business correspondence too. Sure you want personal-only mapping? I'll filter business mail out aggressively."

**Q2:** **HE:** "במשפט: מי אתה?" / **EN:** "In one sentence: who are you?"
- For business: "what do you do for a living?"
- For personal: "describe yourself — student/parent/retired/etc."
- For hybrid: combination

**Q3:** **HE:** "3-5 שמות של אנשים הכי קרובים אליך {בעבודה/בחיים}?" / **EN:** "3-5 names of the people closest to you {at work / in life}?"

Question form changes per use_case: "people you work with" for business; "family + close friends" for personal; both for hybrid.

**Q4 (business + hybrid only — skip for personal):**
> **HE:** "איזה פרויקטים פעילים אצלך עכשיו?" / **EN:** "What projects are active for you right now?"

For `personal`, replace Q4 with: "What's stressing you out lately? (recurring things you want help managing — bills, scheduling, family events, etc.)"

Persist all answers verbatim into `_meta/log.md`. Use them to bias entity extraction (these names get pre-seeded into `context/people/`).

### Step 0.5 — Confirm path and scope

Default vault path is `~/Business`. Ask: "להשתמש בנתיב הזה או אחר?" / "Use this path or another?" Confirm the user understands: ~15 minutes, all data local, `raw/` will be immutable.

---

## Phase 1 — Ingest 90 days into `raw/` (≤5 min)

**Discipline:** ingest is separate from analysis. `raw/` is immutable after this phase. If anything fails mid-ingest, resume from the last saved file. No re-pulling unless `--force-ingest`.

### Gmail
- Pull only sent threads, newer than 90 days, capped at 200 messages on first run (resumable).
- For each message, write `raw/gmail/{ISO-date}-{slug-from-subject}.md` with frontmatter (`from`, `to`, `cc`, `date`, `labels`, `thread_id`) and the message body below.
- **Hard-skip the entire thread** if ANY of the following match (run all four checks per thread; one match → skip + log to `_meta/skipped.md`):
  1. Any label hits the EN/HE token list in `references/skip-tokens.md`.
  2. Any participant's sender/recipient domain matches the domain regex list in `references/skip-tokens.md`.
  3. The subject line contains any EN/HE skip token (case-insensitive substring).
  4. The first 500 characters of the message body contain any EN/HE skip token.
- **Important**: many Gmail MCP implementations expose only IMAP-style labels (`[Imap]/Sent`, etc.) and hide user-defined labels. Do NOT rely on label-matching alone. Subject + body + domain matching is the actual defense.
- Hebrew subjects: slugify with transliteration via `python-slugify` `lang_code='he'` if installed, else `{ISO-date}-{8-char-hash}.md` with original subject in frontmatter `subject:`.
- On 429 (rate limit): sleep 30s, print one line ("Google rate-limited — pausing"), continue.

### Calendar
- Last 90 days of events where `organizer == user_email` OR `len(attendees) ≥ 2`.
- Write `raw/calendar/{ISO-date}-{slug}.md` with frontmatter `attendees`, `duration`, `title`. Body = description + location.
- Cap at 300 events.

### Drive / Docs
- Walk `list_recent_files`. For docs the user authored or edited in 90d, fetch content, write to `raw/drive/{ISO-date}-{slug}.md`. Skip files >50KB of content. Skip if title matches `^(Untitled|Copy of|Draft\b)` and content is <500 chars.
- If >40% of returned files match those template patterns, **auto-downgrade Drive weight** for entity extraction. Print: "Drive looks template-heavy. Weighting Gmail/Calendar higher."

### Sheets
- For each spreadsheet in window, capture sheet names + first 50 rows of each. Write as a single `raw/sheets/{name}.md` with tables in markdown.

### Slack (optional)
- If Slack MCP connected: list channels the user is a member of, score each by `messages_from_user_in_90d`, take top 3, dump the last 50 messages per channel to `raw/slack/{channel}.md`.
- **Skip rules**: Hard-skip any DM channel by default (1:1 conversations are too personal-leaning). Hard-skip channels whose names contain any token from `references/skip-tokens.md` (legal/medical/finance/HR equivalents). Common Slack-specific skip patterns: `#personal-*`, `#mental-health-*`, `#hr-*`, `#legal-*`, `#salary-*`, `#1on1-*`, `#dm-*`.
- For ingested channels: store one file per channel with a `header` block (channel name, member count, last_active), then chronological messages. Anonymize user IDs to display names via context/people/ matching where possible.
- If Slack export `.zip` provided as fallback: walk the directory, treat each channel JSON as a thread, apply same skip rules.

After Phase 1, count what landed. Update `_meta/confidence.json`. If counts are now below `full` thresholds, recompute mode and note the downgrade.

---

## Phase 2 — Extract entities (≤3 min)

What gets extracted depends on `use_case` from Phase 0.4:

| use_case | extracts | skips |
|---|---|---|
| `business` | people (clients/partners/vendors/colleagues), companies, projects, voice signal | family-only relationships, personal-only projects |
| `personal` | people (close contacts only, ≥3 interactions), voice signal | companies, projects |
| `hybrid` | everything, but every entity tagged `scope: business \| personal` | nothing |

Read `raw/` (cheap — it's local). Extract per the table above.

### People
- From Gmail: `From:`, `To:`, `Cc:` headers across all messages. Group by email address; merge by display-name similarity (≥0.85 Jaro-Winkler) only when domains match.
- From Calendar: `attendees`.
- Rank by interaction count. Take top-50.
- For each person record: canonical name, email(s), domain(s), latest interaction date, interaction count, sample of 3 subject lines.

### Companies
- Group people by email domain. Skip free-mail domains (gmail.com, hotmail.com, outlook.com, yahoo.com, walla.co.il, gmail.co.il, icloud.com).
- For each company: name (derive from domain or from email signatures spotted in `raw/`), domain, list of people, latest interaction date.

### Projects
- Pattern-mine subjects + Drive titles + Calendar event titles for repeated proper nouns and noun phrases that recur in ≥3 distinct items across ≥2 sources.
- Cross-check against the user's Phase 0.4 answers — boost score for names mentioned there.
- Take top-15.

### Voice signal (raw input to Phase 4)
- Pick 50 sent messages, biased toward conversational threads (excluding bulk/template/automated messages).
- Detect contamination: any message with ≥2 of {em-dash density >1.5/100 words; "I hope this email finds you well"/"מקווה שהמייל מוצא אותך טוב" present; structured bullet list of >3 items; "moreover"/"furthermore"/"לסיכום, "} → exclude. Log to `_meta/contamination.md`. Aim for 50 clean; if <30 clean remain, downgrade to `preliminary` and label voice.md `confidence: low`.

Write extracted entities to `_meta/entities.json` (intermediate — not part of final output).

---

## Phase 2.5 — Visual extraction (≤4 min, optional)

**Why this exists.** Without visual extraction, the `design-deck` and `draft-proposal` skills can only produce text outlines. The result is documents that don't look like the user's brand. The audience opens a proposal generated by their like-you agent, sees Times-New-Roman black-on-white, and the magic dies. This phase prevents that.

### Step 2.5.1 — Detect visual sources

Walk `_meta/entities.json` for Drive items with these mimeTypes:
- `application/vnd.google-apps.presentation` (Slides)
- `application/pdf` (PDF proposals/decks if they appear branded)
- `application/vnd.google-apps.document` whose title contains `ממותג` / `branded` / `proposal` / `הצעה` (designed proposals)
- `application/vnd.openxmlformats-officedocument.presentationml.presentation` (.pptx)

Rank by likely brand-fidelity: most-recent first, prefer ones where the user is the owner. Take up to **3 sources** (cap to control runtime + token cost).

### Step 2.5.2 — Tooling check

The visual extraction requires LOCAL image tools. Probe in order:
1. `pdftoppm` (from poppler) — preferred
2. `pdftocairo` — fallback
3. `magick` / `convert` (ImageMagick) — fallback for PDFs only

If NONE installed → write `context/design-system.md` with `confidence: none` body that says: "No image tools detected. Visual extraction skipped. Install via `brew install poppler` to enable." Skip the rest of Phase 2.5.

### Step 2.5.3 — Render to PNG + extract original embedded assets

For each visual source:
1. Use `mcp__claude_ai_Google_Drive__download_file_content` with `exportMimeType: 'application/pdf'`.
2. Decode the base64 payload via `jq -r .content | base64 -d` and write to `raw/visual/decks/{slug}.pdf`.
3. **Size guard**: if the base64 payload >2MB, fall back to `exportMimeType: 'image/jpeg'` which exports only the first slide. Note this in `_meta/log.md` so the user knows the extraction was partial.
4. **Extract embedded images first** (the originals — logos, photos, icons): `pdfimages -all raw/visual/decks/{slug}.pdf raw/visual/assets/{slug}/{slug}` writes each embedded image as its own PNG/JPEG/etc. These ARE the user's persistent reusable assets — no re-creation needed.
5. **Render pages for vision analysis**: `pdftoppm -png -r 110 -f 1 -l 5 {pdf} raw/visual/pages/{slug}` (first 5 pages at 110 DPI for vision token extraction).

### Step 2.5.3b — Hi-DPI fallback for low-res embedded assets

`pdfimages` returns the embedded raster at whatever resolution was stored in the PDF. Google Docs often embeds banner images as ~800×100 raster — fine for screen, **too small for print or for re-embedding in new outputs** (will look fuzzy when scaled).

**Fallback rule** — for each extracted asset that will be used as a structural element (banner, header, logo):
1. Check dimensions via `magick identify`.
2. If `width < 1500px` (rough threshold for "needs to scale to A4 width without softness"), generate a hi-res version:
   ```bash
   pdftoppm -png -r 600 -f 1 -l 1 {pdf} hires-prefix
   # crop the banner region (top N pixels of A4 at 600 DPI = top ~600px for a 25mm banner)
   magick hires-prefix-1.png -crop 5100x600+0+0 +repage -trim +repage raw/visual/assets/{slug}/banner-hires.png
   ```
3. Use the `-trim` flag to remove whitespace beyond the banner edge. The output is typically ~5000×600px — sharp at any zoom.
4. **Persist BOTH versions**: the original embedded asset (small, lossless from the source PDF) AND the hi-DPI re-render (larger, derived). Skills should prefer the hi-DPI version unless explicitly told otherwise.

The decision goes into `_meta/log.md` so the user can audit which version was used and why.

### Step 2.5.3c — Identify ACTUAL embedded fonts (don't guess)

Use `pdffonts` to read the actual embedded font names from the source PDF instead of guessing from vision:
```bash
pdffonts raw/visual/decks/{slug}.pdf
```
This returns font names like `NotoSansHebrew-Regular`, `ArialMT`, `Heebo-Bold`, etc. (often subset-prefixed with `AAAAAA+` etc. — strip the prefix). Cross-reference these against Google Fonts availability so the generated CSS can load the *correct* font, not the LLM's best guess.

If `pdffonts` returns subset fonts only (font files not extractable as TTF), record the font names in `design-system.md`'s `typography` section and load them from Google Fonts CDN. Common Google Docs Hebrew defaults: `Noto Sans Hebrew`, `Heebo`, `Assistant`, `Rubik`, `Open Sans Hebrew`, `Frank Ruhl Libre`, `David Libre`.

### Step 2.5.4 — Extract design tokens via vision

Read the rendered images. For EACH source, extract:

- **Primary brand color** (hex) — the dominant accent used for emphasis (titles, callouts).
- **Secondary/accent colors** (up to 3) — supporting colors.
- **Neutral palette** — background, text, muted.
- **Typography**: from `pdffonts` output (step 2.5.3c), not vision. Record the exact font names found in the source PDF. If subset-prefixed (e.g., `AAAAAA+NotoSansHebrew-Regular`), strip the prefix to get the canonical name. Vision is only used as a backup if `pdffonts` returns no usable names.
- **Logo treatment** — where the logo sits (top-left/top-right), monogram vs full, size relative to slide.
- **Layout patterns** — grid (8/12-col), margins, headline placement (top vs centered), content-area shape.
- **Numerical accents** — does the design use big numbers, callout boxes, percentage labels? Capture conventions.
- **Photographic style** — none, illustration, photography, mixed.
- **Density** — minimal-airy vs dense-info.
- **Distinguishing motif** — anything that screams "this is THEIR brand": signature color combo, signature shape (rounded squares, hairline rules), signature punctuation pattern (period-after-statement, ellipsis-to-call-action). Extracted, not invented.

### Step 2.5.5 — Cross-source synthesis

If multiple sources were extracted: cross-check for consistency. Tokens present in ≥2 sources are `confidence: high`. Single-source tokens are `confidence: medium`. Conflicts get a `variant:` note rather than a single value.

### Step 2.5.6 — Write `context/design-system.md`

Use the template in `references/design-system-template.md`. Frontmatter includes:
- `extracted_from: [list of source filenames]`
- `confidence: high | medium | low`
- `tooling_used: pdftoppm | image-export-only`
- Color values as hex codes with one-word semantic names (primary, accent, ink, paper, etc.)

This file is now load-bearing: every downstream skill that produces visual output reads it.

### Step 2.5.7 — Caveat: this is best-effort, not exact

Vision-extracted fonts are a best guess; exact font matching requires the user's font files. Hex codes are approximate (sampled from rendered pixels, not source CSS). The output is calibrated to be 80%-faithful, not pixel-perfect. The skill should print this caveat to the user:

> **HE:** "חילצתי מערכת עיצוב מ-{N} מסמכים. הצבעים מדויקים, הפונטים זה ניחוש מושכל. תפתח את `context/design-system.md` ותאמת — שינויים שתעשה ידנית יישמרו."
>
> **EN:** "Extracted a design system from {N} sources. Colors are accurate; fonts are educated guesses. Open `context/design-system.md` and validate — manual edits stick."

## Phase 3 — Write `context/` pages (≤2 min)

One file per entity, cross-linked.

### `context/people/{slug}.md`
```yaml
---
name: {canonical name}
slug: {ascii-slug}
emails: [list]
company: {company-slug or null}
relationship: client | partner | prospect | vendor | colleague | unknown
first_seen: {ISO date}
last_seen: {ISO date}
interactions: {count}
aliases: [optional list — for dedup safety]
confidence: high | medium | low
---

## Who they are
{1-3 sentences from extraction, no LLM speculation. Cite source: "9 threads in raw/gmail/, 4 calendar events"}

## How you talk to them
{Style notes if voice signal differs by audience — e.g., "shorter sentences, no sign-off, Hebrew only"}

## Active work
{Reference project pages: see [context/projects/...](...)}

## Notes
{User-editable. Initially empty or pre-seeded from Phase 0.4 orientation answers.}
```

### `context/companies/{slug}.md`
Frontmatter: `name`, `domain`, `people` (list of slugs), `last_seen`. Body: 2-line "what they do" if derivable from Drive/email content, else just the list of contacts.

### `context/projects/{slug}.md`
Frontmatter: `name`, `status: active | dormant | done | unknown`, `last_touch`, `people` (slug list), `companies` (slug list). Body: 3-line description if extractable, else just refs.

### Cross-linking rule
Every reference to an entity inside any markdown body uses a relative path: `[context/people/dan-cohen](../people/dan-cohen.md)`. This lets future agents follow the graph without an Obsidian-style wiki engine.

---

## Phase 4 — Generate `context/voice.md` (≤2 min)

The voice file is the difference between an agent that writes like you and an agent that writes like an LLM.

### Algorithm (hybrid stats + LLM, lock with exemplars)

**Step 4.1 — Deterministic pass.** For the 50 clean sent messages, compute:
- Sentence length: median, p25, p75 (in tokens; use a simple regex tokenizer)
- Sign-off frequency: count of "תודה", "Best", "Thanks", first-name-only, no-signoff (abrupt stop)
- Opening pattern frequency: `^שלום \S+`, `^\S+ —`, `^\S+,`, bare-first-name+question, plain hello
- Em-dash count per 100 words
- Exclamation count per 100 words
- Question density (% of sentences ending in `?`)
- Hebrew/English ratio (% of characters in Hebrew alphabet)
- Top 30 content words by TF-IDF against a baseline Hebrew corpus (if available; otherwise just top-30 by frequency excluding stopwords)
Dump to `_meta/voice.stats.json`.

**Step 4.2 — Qualitative pass.** Feed the 50 messages + `voice.stats.json` to a single LLM call (use the current session, no subagent). Ask it to extract:
- 5 verbatim exemplars chosen to span the user's range: shortest, longest, most casual, most formal, one to a stranger. **Quote verbatim — do not paraphrase.** Each exemplar gets a 1-line label header: `# To a known client, mid-project, friendly`.
- Per-audience register notes (`client/partner/prospect/vendor`) — 2 lines each + 1 exemplar each.
- Intent rules: behaviors extracted with frequency counts. e.g., "8/10 first-replies to prospects name a price. Never asks 'what's your budget' first." or "Always offers a no-pressure escape clause in cold replies." or "CCs partner Elay on every deal >₪50k."

**Step 4.3 — Write `voice.md`.** Use the template in `references/voice-template.md`. Structure:

```
## TL;DR (10 lines max)
## Verbatim exemplars (5)
## Sentence rhythm (from stats)
## Openings & sign-offs (from stats)
## Punctuation fingerprint (from stats)
## Code-switching
## Jargon — yours vs. forbidden
## Register by audience (4 sub-blocks)
## Intent rules (אותה כוונה)
## Forbidden phrases
## Hebrew calque blocklist (inherits hebrew-copy if installed)
## Provenance & confidence
```

**Hebrew calque blocklist** is mandatory if any Hebrew is detected. Hardcoded bans (lift from `references/hebrew-calques.md`):
- `מקווה שהמייל מוצא אותך טוב` — translated "hope this email finds you well"
- `אל תהסס/י לפנות` — bureaucratic, prefer "תכתוב/תכתבי לי" or just stop
- `מצפה לשמוע ממך בקרוב` — calque of "looking forward to hearing from you"
- `הנני`, `אבקש`, `בזאת`, `על מנת`, `מאחר ו-`, `אשר` (use `ש-`) — legal-Hebrew register
- `כדי ל-` + infinitive when `כדי ש-` or a bare verb works

Prefer pattern-interrupt openings (`דן —`, `דן, שאלה קצרה.`) over `שלום דן, מקווה ששלומך טוב`. Prefer fragment rhythm.

---

## Phase 5 — Generate skills (≤2 min)

This is the differentiator. Most agent tools generate content skills. The judgment skills are what makes the system *act* like the user, not just *write* like them.

### Catalog scoping by use_case

| use_case | floor | ceiling | mandatory | excluded |
|---|---|---|---|---|
| `business` | 5 | 10 | reply-email, triage-inbox, prioritize | nothing |
| `personal` | 2 | 4 | reply-email, prioritize | draft-proposal, design-deck, chase-nudge, when-to-escalate, respond-as-me-{client} (all business-only) |
| `hybrid` | 5 | 10 | reply-email, triage-inbox, prioritize | nothing, but every generated skill gets a `scope_filter:` parameter so it can be invoked with `--scope=personal` or `--scope=business` |

For `personal`, the agent should feel light — most users don't want a 10-skill toolkit just to manage their inbox. Generate the minimum that's useful. The `voice.md` work is still the load-bearing piece; everything else is thinner.

### Skill catalog rules

For each skill in `references/skill-catalog.md`, evaluate its `precondition` against `_meta/entities.json` AND check it's not in the `use_case`-excluded list above. Score. Take top-N within the use_case bounds.

| Skill | Folder | Precondition |
|---|---|---|
| `reply-email` | content | always (universal) |
| `draft-proposal` | content | ≥2 quotes/SOWs found in Drive (titles match `proposal|quote|הצעה|הצעת מחיר`) |
| `meeting-followup` | content | ≥5 organized meetings with external attendees |
| `design-deck` | content | ≥1 deck authored in Drive (mimeType `application/vnd.google-apps.presentation` OR title matches `deck|presentation|מצגת`) |
| `chase-nudge` | content | ≥2 follow-up emails detected in sent (same recipient, ≥3 days apart, no reply between) |
| `end-of-day-summary` | content | always |
| `triage-inbox` | judgment | always |
| `when-to-escalate` | judgment | ≥3 distinct sender→reply-latency patterns in sent mail |
| `prioritize` | judgment | always |
| `respond-as-me-{client}` | content | top-quintile interaction frequency AND ≥10 sent emails. Hard cap: 2 of these. |

For each generated skill, write `skills/{folder}/{slug}/SKILL.md` using `references/skill-templates.md`. Each SKILL.md is short (~60 lines): YAML frontmatter, one-paragraph "what this does", a "read this first" pointer to `~/Business/.claude/context/voice.md` and the relevant `context/people/` or `context/projects/`, a 3–5 step procedure, an output format spec.

**Print the catalog to the user before writing:** "מצאתי N חברות, M פרויקטים פעילים, K פגישות שבועיות → אני מייצר את הסקילים האלה: [list]. אישור?" / "Found N companies, M active projects, K weekly meetings → generating these skills: [list]. Approve?"

If user vetoes any, drop and respect the floor of 5.

---

## Phase 6 — Write `automations.md` (≤30 sec)

In v0.1, `automations.md` is a **declarative spec** read by future Claude Code sessions, **not** a live cron daemon. Make this explicit at the top of the file. The trigger daemon ships in v0.2.

Lift the 6 templates from `references/automation-templates.md` and filter to those whose underlying skill was generated in Phase 5. Default set (slide 23):

```
# automations.md
# Declarative spec, v0.1 — not yet executed by a daemon.
# To run manually: in a Claude Code session, say "run my automations" and Claude will
# walk this file top-to-bottom, invoking the listed skills.

@every 1h
  → skills/judgment/triage-inbox      # 6/dom from slide 23: סריקת אינבוקס
  → skills/content/reply-email        from: known-clients
  → skills/content/draft-proposal     from: prospects

@calendar:meeting-ended
  → skills/content/meeting-followup
  # → ping-whatsapp ("how did it go?")    # disabled in v0.1 — needs WA bridge

@email:from(<top-client-email>)
  → skills/content/reply-email        voice: yours

@daily 18:00
  → skills/content/end-of-day-summary
  # → whatsapp                            # disabled in v0.1

@silence 3d
  → skills/content/chase-nudge        ask-first
```

Fill `<top-client-email>` with the highest-interaction client from `_meta/entities.json`.

---

## Phase 7 — Write CLAUDE.md and validate

### CLAUDE.md
Use `references/claude-md-template.md`. Frontmatter must include:
- `generated_by: like-you v0.1.0`
- `generated_at: {ISO}`
- `mode: full | preliminary | cold-start`
- `confidence: {full counts}`

Body (short — every future session reads this first):
1. Identity: one-line summary from Phase 0.4 answer #1.
2. Memory layout: pointers to `context/`, `skills/`, `automations.md`.
3. Voice rule: "Before writing anything addressed to a person, read `context/voice.md` and the relevant `context/people/{slug}.md` (audience-specific register block)."
4. Privacy rule: "Never write anything to `raw/`. Never include skip-list label tokens in output."
5. Three commands the user can issue: `triage`, `reply to <name>`, `prep brief for <calendar event>`.

### Validate
Present **5 specific extractions** to the user with the prompt:

> **HE:** "5 קיצונים שצריך לאשר: 1. דן כהן = dancohen@gmail.com? 2. הפרויקט 'שגיא' הוא של [חברה X]? 3. הקול שלך — קצר, ישיר, ללא חתימה? 4. הסקיל draft-proposal יודע לכתוב כמוך מ-12 הצעות עבר? 5. אישור?"

> **EN:** "5 spot checks: 1. Dan Cohen = dancohen@gmail.com? 2. Project 'Sagi' belongs to company X? 3. Voice = short, direct, no sign-off? 4. draft-proposal skill learned from 12 past proposals? 5. Approve?"

If the user corrects anything, fix it in the file and log the correction to `_meta/log.md`. Do not skip this step.

### Finalize
- Lock `raw/` permissions to read-only.
- Print a 5-line summary: workspace path, mode, counts, the two slash commands the user types next to test (`/skill reply-email`, `/skill triage-inbox`), the path to `_meta/log.md` for the audit trail.

---

## Failure modes — what to do when it goes wrong

These are not "errors." They are events with next actions. Every printed line names what just happened and what to do next.

1. **Gmail returned headers but no bodies** (read-only-metadata scope): "Re-authenticate with full read scope, or drop an `.mbox` — I'll keep going either way."
2. **Google rate-limited mid-pull**: "Pausing 30s, then resuming from message X/Y."
3. **Empty/new account** (<5 sent in 90d): "Looks like a fresh account. Want cold-start mode with the 3 orientation questions, or hook your real account?"
4. **Drive looks AI-template-heavy** (>40% titles match Untitled/Copy of/Draft): auto-downgrade Drive weight, print explanation.
5. **Hebrew subject collision**: files saved as `{date}-{hash}.md` with original in frontmatter.
6. **>12 contaminated emails** in voice sample: "12 of your 50 emails look LLM-assisted. Excluded. Running with N clean."
7. **All 5 MCPs missing**: fall through to cold-start with pasted-sample voice and `seed-questions.md`.
8. **The 15-min budget blows past 20**: pause, print "this is taking longer than promised — your sample is larger than expected. Continue, or stop and ship preliminary?"

---

## References

- `references/claude-md-template.md` — root constitution template
- `references/context-page-templates.md` — person/company/project page skeletons
- `references/voice-template.md` — voice.md skeleton + extraction prompt
- `references/skill-templates.md` — CONTENT + JUDGMENT skill skeletons
- `references/skill-catalog.md` — full skill list with preconditions, scoring rules
- `references/automation-templates.md` — the 6 shipped triggers
- `references/hebrew-calques.md` — bureaucratic-Hebrew blocklist + pattern-interrupt openings
- `references/skip-tokens.md` — privacy hard-skip list (EN + HE), user-extendable
